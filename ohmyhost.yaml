version: 1
project: slop-rooster
build:
  install: npm ci --ignore-scripts
  command: npm run build
  output: dist
runtime:
  mode: edge
  egress:
    allow:
      - https://generativelanguage.googleapis.com
database:
  enabled: true
  migrations: migrations
auth:
  provider: none
mail:
  enabled: false
storage:
  provider: r2
  jurisdiction: eu
  public: false
functions:
  crons:
    - "*/5 * * * *"
